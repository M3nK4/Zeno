# === Server ===
PORT=3000
JWT_SECRET=change-me-to-a-random-string

# === Evolution API ===
EVOLUTION_API_URL=http://localhost:8080
EVOLUTION_API_KEY=change-me
EVOLUTION_INSTANCE=zerox

# === LLM (defaults, can be changed from admin panel) ===
CLAUDE_API_KEY=
OPENAI_API_KEY=

# === SMTP (for handoff email notifications) ===
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@zerox.technology

# === CORS ===
CORS_ORIGIN=*

# === Admin (initial setup — run: npm run create-admin) ===
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me
